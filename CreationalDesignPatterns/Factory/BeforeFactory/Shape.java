//The Factory Method Pattern defines an interface for creating objects, but lets subclasses (or the factory class) decide which object to create.




package CreationalDesignPatterns.Factory.BeforeFactory;

interface Shape {
    void draw();
}
